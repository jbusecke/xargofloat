name: xargofloat_test
channels:
  - conda-forge
dependencies:
  # Required for full project functionality (dont remove)
  - pytest
  # Examples (remove and add as needed)
  - xarray
  - numpy
  - scipy
  - dask
  - netcdf4
  - pandas
  - matplotlib
  - astropy # delete when the requirements for xarrayutils are eased
  - pip
  - pip:
    - git+https://github.com/jbusecke/xarrayutils.git
    # Required for full project functionality (dont remove)
    - codecov
    - pytest-cov
